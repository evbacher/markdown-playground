Some font changes that work (words only): **bold**, _italic_.

Some font changes that don't work (whitespace after): **bold **, _italic _.

Some font changes that don't work (whitespace before): ** bold**, _ italic_.
